const InitialState = {
    id: 2,
    name: 'Сергей П',
    photo: '../img/user2.png'
};

export default (state = InitialState, {type, payload}) => {
    switch (type) {

        default:
            return state
    }
}